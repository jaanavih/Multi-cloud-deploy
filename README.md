# Multi-cloud-deploy

Canonical repo: [github.com/jaanavih/Multi-cloud-deploy](https://github.com/jaanavih/Multi-cloud-deploy).

Default CI uses root **`Jenkinsfile`** (Option A): cost reports → **`input()`** to choose AWS/GCP → deploy. See **`COST_COMPARISON_README.md`**.
